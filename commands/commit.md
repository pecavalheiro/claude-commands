Commit the changes in this branch splitting the commits by cronological and logical order.
Check what depends on what, and commit the dependencies first, in order.
Commit logical changes together with the respective tests.
Ensure commit messages are short and clear.
